---
interface Props {
  title: string;
  introduction?: string;
}

const { title, introduction } = Astro.props;
---

<header class="page-header-shell">
  <div class="page-header container">
    <h1>{title}</h1>
    {introduction && <p class="page-header__introduction">{introduction}</p>}
  </div>
</header>
